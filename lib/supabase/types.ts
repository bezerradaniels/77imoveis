// Tipos do banco (placeholder).
// Quando o Supabase estiver conectado, gere os tipos reais com:
//   npm run db:types
// Isso vai sobrescrever este arquivo com a tipagem completa do banco.
export type Database = any;
